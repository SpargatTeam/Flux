name: Build Windows

on: [push, pull_request]

jobs:
  setup:
    runs-on: ubuntu-latest
    steps:
    - name: Setup Environment
      run: bash setup.sh

    - name: Build Project for Windows
      run: bash make.sh windows