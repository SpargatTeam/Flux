name: Build macOS

on: [push, pull_request]

jobs:
  setup:
    runs-on: ubuntu-latest
    steps:
    - name: Setup Environment
      run: bash setup.sh

    - name: Build Project for macOS
      run: bash make.sh macos